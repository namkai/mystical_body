import React from 'react';
import PropTypes from 'prop-types';
import './View.css'

const Home = (props) => (
  <div className="home">
    <p>test</p>
  </div>
);

Home.propTypes = {};
Home.defaultProps = {};

export default Home;
